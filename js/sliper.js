$(document).ready(function () {
	$('.slider').slick({
		autoplay: true,
		accessibility: false,
		adaptiveHeight: true,
		arrows: false,
		slidesToShow: 6,
		centerMode: true,
		autoplaySpeed: 1000,
	})
})
